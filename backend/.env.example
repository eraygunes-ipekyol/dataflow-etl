# Database
DATABASE_URL=sqlite:///./dataflow.db

# Encryption key for connection secrets (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# CORS
CORS_ORIGINS=["http://localhost:5173"]

# Chunk size for streaming
DEFAULT_CHUNK_SIZE=5000
PREVIEW_ROW_LIMIT=100
